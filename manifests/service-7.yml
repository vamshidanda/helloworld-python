apiVersion: v1
kind: Service
metadata:
    name: "myaskcluster1-a3b2"
    labels:
        app: "myaskcluster1-a3b2"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "myaskcluster1-a3b2"